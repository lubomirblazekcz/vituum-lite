{% set description = 'bar' %}

<article>
    <h2>{{ title }}</h2>
    <p>{{ description }}</p>
</article>

{% for item in 1..3 %}
    {% include 'src/components/article.twig' with {title: 'Hello', description: 'There'} %}
{% endfor %}
